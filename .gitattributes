# 텍스트 파일은 저장소에 LF로 저장한다.
* text=auto eol=lf

# 배치 파일은 반드시 CRLF여야 한다.
# LF로 저장되면 cmd.exe가 명령어를 잘라먹어서 (ipconfig -> onfig) 서버가 뜨지 않는다.
*.bat text eol=crlf

# 이미지는 변환하지 않는다.
*.jpg  -text
*.jpeg -text
*.png  -text
*.webp -text
*.gif  -text
